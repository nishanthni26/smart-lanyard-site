# Official Motion Repository Installation Note

Source: https://github.com/motiondivision/motion

The repository identifies Motion as a modern animation library for React and JavaScript. Its React / JavaScript installation guidance is:

```bash
npm install motion
```

The current project already uses `framer-motion`, which provides the React component API. The official `motion` package can be installed alongside it for low-level JavaScript/DOM animation APIs if required.
